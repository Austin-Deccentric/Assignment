[project]
name = "assignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "0xtin", email = "caugust19.ac@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
]

[project.scripts]
assignment = "assignment:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
